[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pyfuncs-to-chars"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["flask"]

[tool.setuptools.packages.find]
where = ["."]
include = ["core*"]
